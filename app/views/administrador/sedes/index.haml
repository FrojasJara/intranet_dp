= datatable_src

.sectionHeader
    %h1 Mantenedor de Sedes
    = link_to raw("#{t 'crud.new'} Sede"), new_administrador_sede_path, class: "btn btn-primary"


%table#example.table
    %thead
        %tr
            %th ID
            %th Nombre
            %th Direccion
            %th Telefono
            %th Email

    %tbody
        - @items.each do |i|
            %tr
                %td= i.id
                %td
                    .btn-group
                        %a.dropdown-toggle{href: '#', data: {toggle: 'dropdown'}}
                            = i.nombre
                            %span.caret
                        %ul.dropdown-menu
                            %li= link_to raw(t('crud.show')), administrador_sede_path(i)
                            %li= link_to raw(t('crud.edit')), edit_administrador_sede_path(i)
                            %li= link_to raw(t('crud.delete')), administrador_sede_path(i), :confirm => t('crud.confirm'), method: :delete
                            %li.divider
                            %li= history i, t(:historial) 
                
                %td= i.direccion
                %td= i.telefono
                %td= i.email
