#obtener-horario
    .page-header
        %h1 Horario de clases de asignaturas inscritas en el semestre    
    = render "alumnos/alumnos/partials/seleccion_plan"
    = render "alumnos/alumnos/partials/informacion_tabulada_alumno"    
    - if @existen_datos
        = render partial: "alumnos/alumnos/partials/horario", locals: {horario: @horario}
    - else
        = notificaciones